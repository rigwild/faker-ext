{
  "name": "faker-extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --outdir=dist src/*.ts && rm dist/lib.js && cp src/manifest.json dist/",
    "build:watch": "esbuild --bundle --watch --outdir=dist src/*.ts"
  },
  "devDependencies": {
    "esbuild": "^0.13.8"
  }
}
